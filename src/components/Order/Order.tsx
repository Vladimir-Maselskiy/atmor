'use client';
import React from 'react';
import { Box } from '../Box/Box';
import { UserForm } from '../UserForm/UserForm';

export const Order = () => {
  return (
    <Box py={115}>
      <UserForm />
    </Box>
  );
};
